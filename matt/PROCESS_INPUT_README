// To use processInput.h to turn an input file into an adjacency matrix or
// list, do the following:

// Put this near the top of your program:
#include "processInput.h"

// Use this to construct an adjancey for a given string "filename"
AdjMatrix adjM (filename);    // gets matrix corresponding to input
AdjList adjL (filename);        // gets list corresponding to input

// To get an idea for what you can do with these Adjacencies, look at the
// header comments in processInput.h
